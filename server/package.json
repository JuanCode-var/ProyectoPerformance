{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch index.ts",
    "start": "node dist/index.js",
    "worker:dev": "cross-env DOTENV_CONFIG_PATH=server/.env nodemon -r dotenv/config src/pagespeed.worker.js",
    "build:server": "tsc -p tsconfig.server.json",
    "build": "tsc -p tsconfig.server.json"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "mongoose": "^8.16.4",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.5",
    "worker": "cross-env DOTENV_CONFIG_PATH=server/.env node -r dotenv/config src/pagespeed.worker.js"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.3.0",
    "@types/nodemailer": "^7.0.0",
    "nodemon": "^3.1.0",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2"
  }
}